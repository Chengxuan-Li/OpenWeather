# Minimal setup.py for deployment compatibility
from setuptools import setup

setup(
    name="openweather",
    version="0.1.0",
    description="NSRDB to EPW web application",
    python_requires=">=3.11",
)
